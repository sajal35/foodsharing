import 'package:flutter/material.dart';

Widget terms(BuildContext context) {
  return Container(
    color: Colors.grey.shade100,
    height: 120,
    alignment: Alignment.center,
    child: Text(
        "To start using the Share Meals app, please login using your email address. if you don't have and account yet, please sign up first.",
        style: TextStyle(color: Colors.blueGrey),
        ),
  );
}
